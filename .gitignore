node_modules/
dist/
coverage/
gauntlet/*.png
gauntlet/report.md
gauntlet/summary.md
.env
.env.*
.vercel/
playwright-report/
test-results/

playwright-report/
test-results/
gauntlet/state.json
gauntlet/evidence/

# Lane scratch: temporary Playwright configs and debug scripts created by
# parallel build agents. Kept out of the repo rather than deleted, because the
# lanes that own them may still be running.
kbdbg.mjs
pw.*.ts
pw.*.mjs
pw.*.config.ts

# INTERNAL BUILD PROCESS — never goes to GitHub.
# These are our working artifacts, not the product: the user's own verbatim bug
# reports, the escalation write-ups, the computed ticket board and the judge's
# working notes. They are load-bearing for scripts/tickets.mjs and the gauntlet,
# so they stay ON DISK and are simply untracked.
gauntlet/user-reported.json
gauntlet/queue.md
gauntlet/tickets.json
gauntlet/known-false-passes.json
gauntlet/judge-notes.md
gauntlet/judge-verdicts.json
gauntlet/escalations.md
docs/tickets.md
docs/audit.md
docs/coverage-audit.md
gauntlet/memwatch.log
gauntlet/memwatch-ALERT.txt
dist-play/
tests/e2e/scratch/

# A private handoff note, not a project document. Kept on disk, never pushed.
docs/SUBMIT.md

# Throwaway feel-test builds served on a side port; never committed.
dist-feel/
