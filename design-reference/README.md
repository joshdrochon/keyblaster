Direction only. Nothing here is an acceptance criterion and nothing here is loaded by the game.
- ui/   — Claude Design interface comps (exported HTML). Redrawn in vector by the build.
- refs/ — reference images for drawing vector assets (e.g. refs/lantern-topdown.png, D90). Looked at, never loaded.
